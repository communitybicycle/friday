import React, { FunctionComponent } from "react";
import Container from "../components/Container";

const Notes: FunctionComponent = () => {
  return <Container>Notes page</Container>;
};

export default Notes;
