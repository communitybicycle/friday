import React from "react";
import { hot } from "react-hot-reload";

interface Props {}

const DashboardEdit: React.FC<Props> = (props) => {
  return <div></div>;
};

export default hot(module)(DashboardEdit);
